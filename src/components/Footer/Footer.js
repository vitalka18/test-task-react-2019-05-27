import React from 'react';

import './Footer.scss';

/**
 * Base application's footer markup.
 */
const Footer = () => {
  return (
    <footer className="footer">
      <p>
        Adyax specializes in working with multi-national, multi-brand companies on a wide range of
        customer, client and employee-facing solutions
      </p>
    </footer>
  );
};

export default Footer;
